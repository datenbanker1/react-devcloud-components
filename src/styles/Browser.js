const style = theme => materialUiTheme => ({
  sortHolder: {
    backgroundColor: "#fff"
  },
  elementsHolder: {
    padding: "8px",
    backgroundColor: "#fff"
  },
  paginationHolder: {
    backgroundColor: "#fff"
  }
});

export default style;
