const style = theme => materialUiTheme => ({
  holder: {
    backgroundColor: "#fff",
    margin: "-8px -8px 0px",
    padding: "8px",
    zIndex: 100,
    whiteSpace: "nowrap"
  }
});

export default style;
