const style = theme => materialUiTheme => ({
  holder: {
    backgroundColor: "#fff",
    margin: "-8px",
    padding: "8px",
    whiteSpace: "nowrap",
    overflow: "scroll"
  }
});

export default style;
